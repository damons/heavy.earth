[package]
name = "dnd-rs"
version = "0.1.0"
edition = "2024"
description = "A native Rust recreation of classic DOS DND"
publish = false

[dependencies]
crossterm = "0.29"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
lto = true
strip = true

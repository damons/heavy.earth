[package]
name = "heavy-earth"
version = "0.1.0"
edition = "2024"
publish = false
description = "HEAVY.EARTH — an ink-and-earth isometric dungeon crawler"

[dependencies]
dnd-rs = { path = "../vendor/dnd" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tiny_http = "0.12"
